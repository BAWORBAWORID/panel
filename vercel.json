{
  "builds": [
    {
      "src": "docker-compose.yml",
      "use": "@now/static-build"
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "docker-compose.yml" }
  ],
  "env": {
    "PORT": "80",
    "HTTPS_PORT": "443"
  }
}
